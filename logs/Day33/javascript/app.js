let person = {
    name: 'Pogi',
    age: 20,
    school: 'USTP',
};

console.log(`Hello World`);
console.log(`Hello ` + person.name );
console.log(person.name + ` is ` + person.age + ` years old`);
console.log(`${person.name} is a ${person.school} student`);